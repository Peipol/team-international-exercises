{
  "name": "interview",
  "version": "1.0.0",
  "description": "toy project for demostration",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf build/ && tsc",
    "start:1": "rm -rf build/ && tsc && node ./build/task/task-1",
    "start:2": "rm -rf build/ && tsc && node ./build/task/task-2",
    "start:3": "rm -rf build/ && tsc && node ./build/task/task-3",
    "test": "jest"
  },
  "author": "Peipol",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^16.11.10",
    "eslint": "^8.3.0",
    "jest": "^27.4.5",
    "prettier": "^2.5.0",
    "ts-jest": "^27.1.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  }
}
